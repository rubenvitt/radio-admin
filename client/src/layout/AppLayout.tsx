import { Outlet } from 'react-router-dom';

// Temporary shell — the full responsive AppLayout (Sider/Drawer nav, theme
// toggle, logout) is implemented in Task 5.8.
export function AppLayout() {
  return <Outlet />;
}
